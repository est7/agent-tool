# tests

Repository-level automated tests for `agent-tool`.

## Usage

- Run all shell regression tests: `bats tests`
- Run a single file: `bats tests/1mcp_status.bats`

Current coverage here focuses on behavior that should be validated against the repository scripts themselves.
